services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
      - GEMINI_API_KEY=your gemini api key here
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
